using LinearAlgebra
using Plots

A1 = []  # The buck converter model when switch is on
A2 = []  # Buck convereter model when switch is off

B1 = []  # Buck convereter model when switch is on
B2 = []  # Buck converter model when switch is off